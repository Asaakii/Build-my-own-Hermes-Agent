# 学习日志

> 项目仓库：[Asaakii/Build-my-own-Hermes-Agent](https://github.com/Asaakii/Build-my-own-Hermes-Agent)

## 2026-05-10

### 阶段 0.1：项目环境与安全基线

- 完成独立 Python 3.12 开发环境和可安装项目骨架。
- 建立依赖、测试与密钥隔离的基础约定。
- 验证结果：依赖一致性检查通过；最小导入测试通过；本地 `.env` 已被 Git 忽略。
- 关键理解：Agent 功能开发之前，先确保运行环境可重复、自动化测试可执行、密钥不会进入版本控制。

### 阶段 0.2：核心数据流与安全边界

- 明确消息、工具调用、工具结果、任务状态与会话的职责边界。
- 建立受限工作区、工具轮次、写入大小、模型超时和命令策略的初始规则。
- 验证结果：15 项测试通过；非法工具名、空内容、错误会话项和非法工具轮次均被拒绝。
- 关键理解：模型的请求与工具的执行结果必须是不同类型的数据；所有外部输入在进入系统时完成验证。

### 阶段 1.1.1：安全模型配置

- 完成模型供应商、模型名称、服务地址、API Key 和超时的统一加载与校验。
- 验证结果：30 项测试通过；真实配置检查只显示脱敏摘要；无效超时和示例 API Key 被拒绝。
- 关键理解：模型配置属于外部输入，必须在进入客户端前转换为已验证的数据对象；`.env` 只提供默认值，终端显式变量优先。

### 阶段 1.1.2：可测试模型客户端

- 完成 OpenAI 兼容纯文本请求、标准化回答提取和统一错误类型。
- 验证结果：41 项测试通过；真实模型连接验证成功；超时、网络、空回答和 HTTP 状态错误均有离线测试覆盖。
- 关键理解：模型 SDK 是可替换的外部依赖；通过注入假客户端，Agent 核心逻辑无需真实网络也能稳定验证。

### 阶段 1.2.1：多消息模型接口

- 模型客户端能够将完整、已验证的会话历史按顺序发送给模型。
- 验证结果：47 项测试通过；空会话、非消息对象与未实现的工具消息均被拒绝；真实模型请求成功。
- 关键理解：连续对话的上下文不是单个字符串，而是有角色和顺序的消息序列。

### 阶段 1.2.2：最小文本 Agent Loop

- 完成内存态会话、任务状态与模型请求的协调；成功时按顺序保留用户和助手消息。
- 验证结果：52 项测试通过；覆盖单轮、多轮历史传递、模型失败和空输入路径。
- 关键理解：Agent Loop 的职责是协调状态与外部模型调用；失败时必须保留真实输入、明确标记失败，不能编造不存在的助手结果。

### 阶段 1.3.1：工具定义与参数校验

- 完成工具声明、风险等级、模型可见工具描述和受限 JSON 参数模式。
- 验证结果：65 项测试通过；未知工具、重复登记、缺失参数、额外参数、错误类型和未实现的复杂参数类型均被拒绝。
- 关键理解：模型提出的工具名称和参数只是外部输入；注册表必须先按预先声明的规则验证，之后才能考虑执行。

### 阶段 1.3.2：注册表执行与工具结果

- 已登记工具只能通过注册表校验后执行预先绑定的 Python 函数。
- 验证结果：72 项测试通过；未知工具、参数错误、预期失败、未预期异常和无效返回值均会得到结构化失败结果。
- 关键理解：模型不应获得任意函数或异常细节；执行层只暴露受控能力，并将观察结果统一为 `ToolResult`。

### 阶段 1.4.1：结构化工具消息协议

- 会话消息已区分普通文本、助手工具请求和携带调用标识的工具结果。
- 验证结果：81 项测试通过；非法字段组合、错误工具调用容器和纯文本客户端误发送工具消息均被拒绝。
- 关键理解：工具调用不是普通文本提示词；调用标识使工具结果能准确回到发起它的助手请求。

### 阶段 1.4.2：假模型工具循环

- 完成“模型请求工具→注册表执行→工具结果写回→模型再次决策”的离线闭环。
- 验证结果：88 项测试通过；覆盖成功调用、未知工具观察、最大轮次停止、错误角色和模型服务失败。
- 关键理解：工具执行失败不一定立刻终止任务；它也是一条观察结果，可由模型在下一次决策中处理。

### 阶段 1.4.3：OpenAI 兼容工具调用适配

- 完成结构化会话、工具定义和模型工具调用响应之间的 OpenAI 兼容转换。
- 验证结果：95 项测试通过；真实 DeepSeek 请求成功返回 `echo_text` 工具调用及预期参数。
- 关键理解：兼容 API 的文本请求成功并不能证明工具调用可用；必须单独验证工具定义、调用标识和 JSON 参数。

### 阶段 2.1.1：受限工作区路径边界

- 完成项目内工作区配置与相对路径解析；工作区默认限制为 `sandbox_workspace`。
- 验证结果：105 项测试通过；绝对路径、父目录穿越、项目根目录、缺失目录和符号链接逃逸均被拒绝。
- 关键理解：路径校验不能只检查字符串；必须在解析符号链接后再次确认目标仍位于工作区内。

### 阶段 2.1.2：只读观察工具

- 完成受限工作区内的目录列举、UTF-8 文本读取和字面量检索能力。
- 验证结果：117 项测试通过；越界路径、外部符号链接、二进制内容、缺失文件、超大文件、超量路径和超量检索结果均被受控处理。
- 关键理解：限制返回结果不等于限制实际资源消耗；检索工具还必须限制目录遍历和文件扫描过程。

### 阶段 2.2.1：创建型受控写入

- 完成受限工作区内新建 UTF-8 文本文件的能力，并登记为写入风险等级工具。
- 验证结果：124 项测试通过；已有文件不会被覆盖，越界路径、缺失父目录、空字节与超出大小限制的内容均被拒绝。
- 关键理解：独占创建可以防止静默覆盖，但风险等级标签本身不是用户授权；确认令牌与审计仍属于后续阶段。

### 阶段 2.2.2：精确替换与补丁预览

- 完成工作区内 UTF-8 文本的唯一精确替换，并返回受限统一差异预览。
- 验证结果：130 项测试通过；未命中、重复命中、二进制文件和模拟原子替换失败均不会修改原文件。
- 关键理解：写入安全不仅是限制路径；还要让修改目标唯一、让失败不产生半写入，并把实际变更以受限差异呈现。

### 阶段 2.3.1：Pytest 命令策略

- 完成受限工作区测试目标到固定 `python -m pytest -q` 参数数组的转换；本步不启动子进程。
- 验证结果：137 项测试通过且无 warning；任意命令、绝对路径、父目录穿越、缺失目标和非 Python 单文件均被拒绝。
- 关键理解：安全执行的第一步不是过滤危险字符串，而是根本不接受模型提供的完整命令，只由程序构造固定参数数组。

### 阶段 2.3.2：受控 Pytest 执行器

- 完成固定参数 Pytest 的受控执行与工具登记；进程固定在受限工作区中运行，使用 `shell=False`、10 秒超时和非零退出码观察。
- 验证结果：专项 39 项、全量 144 项测试通过；覆盖固定子进程选项、非零退出、超时、进程启动失败、回传输出截断和注册表执行。
- 关键理解：固定参数数组防止模型获得通用 shell 控制，但 `pytest` 本身会执行测试中的 Python 代码；当前能力仅适用于自有练习工作区，不能称为不可信代码沙箱。输出截断仅限制回传模型的文本，不是进程级内存或资源隔离。

### 阶段 2.4.1：编码任务报告契约

- 复用既有任务生命周期，定义工具轮次摘要、测试验证状态和最终编码任务报告。
- 验证结果：专项 13 项、全量 157 项测试通过；完成任务若未通过测试会被拒绝，失败和受阻任务保留真实的通过、失败或未运行验证状态。
- 关键理解：任务状态不应在不同模块重复定义；最终报告应绑定可观察的工具结果与验证状态，而不是只记录模型自评。

### 阶段 2.4.2：离线编码闭环

- 使用预设模型响应完成读取失败测试、精确修改、运行受控 Pytest 与生成最终报告的可重复流程。
- 验证结果：专项 13 项、全量 163 项测试通过；模型未运行测试时只能得到受阻报告，测试失败时得到失败报告，练习工作区外文件保持不变。
- 关键理解：编码任务是否完成由实际测试结果决定，而非模型的最终文本；多工具调用必须按模型响应的轮次归组。当前验证状态从受控 Pytest 输出的稳定状态行读取，后续可演进为机器可读的工具结果字段。

### 阶段 3.1.1：SQLite 模式初始化

- 完成项目内 SQLite 状态数据库的路径校验、可重复初始化、模式版本与会话、消息、任务、工具结果、报告表结构。
- 验证结果：专项 8 项、全量 171 项测试通过；绝对路径、父目录穿越、非 `data/` 目录和错误后缀均被拒绝，重复初始化不破坏模式版本。
- 关键理解：持久化首先要约束数据库位置和模式版本；数据库文件是本地运行状态，不应提交到 Git。

### 阶段 3.1.2：会话与任务读写

- 完成会话消息、结构化工具调用、任务状态、工具结果和最终报告的 SQLite 序列化、保存与恢复。
- 验证结果：专项 15 项、全量 178 项测试通过；重复保存不会重复累积历史，任务必须关联已保存会话，不可 JSON 序列化的工具参数会被拒绝。
- 关键理解：持久化不是直接存 Python 对象；需要稳定序列化协议、事务中的替换语义和外键关联，才能让恢复结果重新满足领域对象校验。

### 阶段 3.1.3：损坏消息记录与隔离恢复

- 完成带跳过计数的会话恢复；单条无法重新构造成消息的记录会被跳过，正常消息按原顺序保留。
- 验证结果：SQLite 专项 19 项、全量 182 项测试通过；覆盖非法角色、非法工具调用 JSON、会话隔离与不存在会话。
- 关键理解：恢复容错必须让数据丢失可见，因此返回跳过数量；当前仅容错消息历史，任务和报告损坏仍应明确报错，不能伪装为正常恢复。

### 阶段 3.2.1：固定 Prompt Builder 与预算策略

- 完成安全规则、工作区规则、工具定义、技能、经授权长期记忆、历史与任务摘要的固定上下文顺序。
- 验证结果：专项 9 项、全量 191 项测试通过；旧历史可由注入摘要器替换，最近的工具请求与工具结果作为完整单元保留。
- 关键理解：超预算时不能静默丢上下文。摘要失败或未配置摘要器会保留原始历史并显式标记超预算；数据库、工具和安全规则不因预算而被截断。

### 阶段 3.2.2：运行时上下文接入与摘要调用

- 将 Prompt Builder 接入文本与工具 Agent 的实际模型请求；工具 Agent 保持独立的结构化工具定义传递。
- 验证结果：专项 3 项、全量 194 项测试通过；长历史中的关键项目代号可经带来源标记的摘要进入模型请求并得到回答。
- 关键理解：本步验证的是可替换摘要服务的运行时边界，不等于已证明任意真实模型摘要都可靠。真实服务发生超时、遗漏或幻觉时，仍要依赖既有失败降级和后续模型错误恢复策略。

### 下一步

- 进入阶段 3.3，实现经授权的长期记忆。
