# 从零构建 Hermes Agent：学习开发计划

> 版本：v1.0  
> 制定日期：2026-08-10  
> 项目仓库：[Asaakii/Build-my-own-Hermes-Agent](https://github.com/Asaakii/Build-my-own-Hermes-Agent)  
> 学习方式：每次只完成一个小步骤；代码由学习者手写，助手说明目标、设计原因、实现顺序与验证方法；通过验证后才进入下一步。若学习中发现计划的假设不成立，会先说明原因，再更新本计划与学习日志。

## 1. 可行性评估与学习范围

### 1.1 结论

**从零构建“学习版 Hermes Agent”可行性高；复刻官方 Hermes Agent 则不适合作为当前目标。**

这里的“高可行性”有明确前提：构建一个面向单个本地工作区、调用现成模型、能读写受限文件、执行受控命令、保存会话、使用记忆与技能的 Python Agent。它将学习 Hermes 的核心设计，而不是复制官方产品的全部平台能力。

已确认的事实：

- [learn-hermes-agent](https://github.com/longyunfeigu/learn-hermes-agent) 已将 Hermes 风格系统拆成 27 个渐进章节，覆盖 Agent 循环、工具、SQLite 会话、记忆、技能、上下文压缩、安全、网关、调度、MCP 与评估等主题。
- [NousResearch/hermes-agent](https://github.com/NousResearch/hermes-agent) 是规模更大的生产级项目，包含多模型、CLI/TUI、多消息平台 Gateway、多终端后端、调度、技能与记忆自改进、MCP、浏览器、语音、轨迹与评估等模块。
- 前一项目已经完成了模型调用、工具调用、会话/长期记忆、技能、Telegram、Gateway/CLI、SQLite 和安全边界等基础。这会降低理解成本，但本项目仍应独立实现核心机制，不能直接复制前一项目的代码。

合理判断：对 Agent 开发学习最有价值的不是“功能数量”，而是理解这条闭环：**任务 → 上下文 → 模型决策 → 受控工具执行 → 观察结果 → 测试/修正 → 持久化经验**。本计划围绕这条闭环安排。

### 1.2 不做什么

以下内容不进入第一版的必做范围：

- 不复刻官方全部渠道、TUI、网页、语音、视觉、云端或 Docker/SSH/Daytona 等执行后端。
- 不允许 Agent 获得不受限制的本机 shell 权限，不把“能执行任意命令”当作完成标准。
- 不做 RL 训练、自主修改自身核心代码、无人审批的自动部署或真实生产运维。
- 不以“模型回答看起来聪明”代替可重复的测试与安全验证。

这些方向会保留为完成核心学习版后的可选扩展；它们不是当前阶段的前置条件。

### 1.3 第一版验收目标

完成后，`HermesLite` 应能在指定的测试工作区中：

1. 通过 CLI 接收一个自然语言编程任务。
2. 在受限工作区内列目录、检索、读取、创建或修改文本文件。
3. 以结构化工具调用的方式运行白名单测试命令，并读取结果。
4. 根据工具观察结果继续推理，直到完成、失败或达到明确上限。
5. 保存会话、任务摘要、经明确授权的长期记忆和可复用技能。
6. 重启后恢复会话，并能回忆此前任务的关键结论。
7. 拒绝越过工作区、危险命令、未授权写入和不合法工具参数。
8. 用自动化测试和一次端到端“修复小项目”任务验证以上行为。

## 2. 学习项目约定

### 2.1 技术选择

- 语言：Python 3.12。
- 模型：使用 OpenAI 兼容接口；开始时继续使用已经可用的 DeepSeek 配置即可，代码保持供应商可替换。
- 持久化：SQLite；它足够轻量，也能帮助理解会话、任务、记忆和审计记录的边界。
- 测试：pytest。
- 命令行：标准库 `argparse` 起步；交互体验不是核心，完成主闭环后再考虑 Rich 或 prompt_toolkit。
- 工作区：默认只允许操作项目下明确配置的 `sandbox_workspace/`，而非电脑任意目录。

### 2.2 文档与提交约定

项目建立后维护以下资料：

- `Hermes-Agent-学习开发计划.md`：本计划，公开提交；每完成一个步骤同步更新状态。
- `learning-log.md`：公开提交，延续前一项目的客观、简洁、统一格式；不写“我”，不记录低级拼写失误，也不写具体创建了哪些文件。
- `实际指导过程复盘.md`：仅本地保存，记录实际操作与排错经验；对 API Key、聊天内容、机器路径、账号、用户标识等进行脱敏，并加入 `.gitignore`。
- `.env`：仅本地保存，加入 `.gitignore`；仓库只提交 `.env.example`。

每个已完成步骤都应包含：代码注释、最小正向验证、至少一个边界/失败验证、`git diff --check`、相关 pytest 测试。提交信息使用中文。助手未经明确授权不代写代码；一旦明确授权由助手实现，也必须补齐测试、日志与说明。

### 2.3 每一步的固定节奏

1. 先说明本步要解决的实际问题，以及为什么现在需要它。
2. 给出小范围代码结构和关键代码的逐段解释，由学习者手写。
3. 先跑编译/静态检查，再跑正向验证和失败验证。
4. 检查结果后，更新计划、学习日志与本地复盘，再提交并推送。
5. 若验证失败，先定位失败属于配置、数据、模型、工具策略还是代码，再修复；不跳过失败直接进入下一步。

## 3. 总体架构目标

```text
CLI 任务输入
    ↓
任务会话 / 上下文构建 ──→ 长期记忆、技能、任务摘要
    ↓
Agent Loop ──→ 模型（仅获得允许的工具定义）
    ↓                         ↑
工具注册表 → 安全策略 → 受限工作区 / 测试运行器
    ↓                         │
结构化工具结果 ────────────────┘
    ↓
会话、任务、审计记录持久化（SQLite）
```

这里最重要的边界是：模型只负责提出工具调用；工具层负责验证、执行、记录和拒绝。模型输出不能直接成为本机命令或文件路径。

## 4. 分阶段学习路线

### 阶段 0：建立学习边界与可运行骨架

#### 0.1 创建独立项目与基础环境（已完成）

- **目标**：创建独立 Git 仓库、Python 3.12 虚拟环境、依赖清单和基本目录，不污染前一项目。
- **手写内容**：最小包结构、`pyproject.toml` 或 `requirements.txt`、`.gitignore`、`.env.example`、环境检查脚本。
- **为什么**：Hermes 风格项目会逐步增加模块；先使用可安装的包结构，后续 CLI、测试和模块导入不会混乱。
- **验证**：新建虚拟环境后可安装依赖；`python --version` 为 3.12；`python -m pytest -q` 能运行；`git check-ignore -v .env` 证明密钥不会被提交。
- **完成标志**：空项目可在另一份临时虚拟环境中重新安装并通过最小测试。

**完成记录（2026-08-10）**：已使用 Python 3.12 虚拟环境完成可编辑安装；依赖一致性检查通过；最小导入测试通过；`.env` 与本地复盘文件均已确认被 Git 忽略。独立临时虚拟环境重装验证留待阶段 6 的可复现性验收统一执行。

#### 0.2 绘制核心数据流与威胁模型

- **目标**：先定义 `Message`、`ToolCall`、`ToolResult`、`TaskState`、`Session` 这些数据在何处产生、保存和消费。
- **手写内容**：一份简短架构说明和最小数据模型；列出允许动作、禁止动作、必须确认的动作。
- **为什么**：后续重构最常见的问题是“状态放错层”。先划边界，才能知道会话记录、长期记忆和工具日志不能混在一起。
- **验证**：能用一张图或表说明一次“读取文件→修改→测试”的数据流；测试数据模型能拒绝空角色、非法工具名和非文本内容。
- **完成标志**：明确默认工作区、命令白名单、最大工具轮数、单次写入大小和模型超时策略。

### 阶段 1：形成可观察、可停止的 Agent 核心

#### 1.1 配置与模型客户端

- **目标**：安全读取模型配置，并把一次模型请求封装为可测试函数。
- **手写内容**：配置加载、配置校验、OpenAI 兼容模型客户端、统一异常类型和脱敏日志。
- **为什么**：模型供应商、模型名、超时和密钥不应散落在 Agent 循环中；统一封装才能替换模型和稳定测试异常分支。
- **验证**：真实模型可回答一句固定问题；缺少配置、无效 Key、超时或空回答时给出明确且不泄露密钥的错误；用假客户端测试成功和失败路径。
- **完成标志**：调用方只接收标准化的模型回答或标准化异常。

#### 1.2 最小 Agent Loop

- **目标**：实现“发送消息→模型要求工具或给出回答→追加观察→继续”的循环。
- **手写内容**：会话内消息列表、最大轮数、停止条件、最终回答返回值；先用一个 `get_current_time` 或 `calculate` 工具验证闭环。
- **为什么**：这不是普通聊天程序；工具结果必须作为新的上下文回到模型，模型才有机会根据真实观察继续行动。
- **验证**：模型能调用一次工具并解释结果；连续工具调用能在上限内停止；模型没有工具调用时直接结束；超过上限时安全失败。
- **完成标志**：终端能清楚显示“正在使用工具”，且每轮调用有结构化记录。

#### 1.3 工具注册表与参数校验

- **目标**：用统一注册表声明工具名称、用途、JSON 参数模式、执行函数和风险级别。
- **手写内容**：`ToolRegistry`、工具定义导出、参数验证、未知工具拒绝和工具结果格式。
- **为什么**：把工具写成连续 `if/else` 会使能力扩展、权限控制与测试越来越困难；注册表是 Hermes 类系统的能力边界。
- **验证**：列出可用工具；合法参数成功；未知工具、缺失参数、额外危险参数和不符合类型的参数都被拒绝。
- **完成标志**：模型看到的工具定义来自注册表，而执行器不相信模型提供的任意名称和参数。

### 阶段 2：让 Agent 在安全工作区完成小型编码任务

#### 2.1 受限工作区与只读工具

- **目标**：实现 `list_files`、`read_file`、`search_text`，且只能访问配置的工作区。
- **手写内容**：路径规范化、相对路径验证、文件大小上限、二进制文件处理、文本检索结果截断。
- **为什么**：编程 Agent 的第一步是观察项目；“读取本机任意路径”不是必需能力，也是明显风险。
- **验证**：能列出和读取工作区样例；拒绝 `../`、绝对路径、符号链接逃逸、过大文件及工作区外路径。
- **完成标志**：每次读取均有审计记录，且工具结果不超过上下文预算。

#### 2.2 受控写入与补丁机制

- **目标**：实现创建和修改工作区文本文件的能力，优先以精确替换或统一补丁表达修改。
- **手写内容**：`write_file`、`apply_patch` 风格工具、写前校验、备份/原子写入策略、差异预览。
- **为什么**：直接让模型覆盖整个文件，容易丢失代码和注释；补丁式修改更容易审查、测试和回滚。
- **验证**：成功创建和修改样例；目标不存在、匹配内容不唯一、超出尺寸、写入工作区外和二进制写入均拒绝；失败后原文件保持不变。
- **完成标志**：工具返回修改摘要与差异统计，而不是静默写入。

#### 2.3 测试运行器与命令策略

- **目标**：Agent 能运行有限的测试命令，例如 `python -m pytest`，并读取截断后的标准输出与退出码。
- **手写内容**：命令令牌化、白名单、禁止 shell 拼接、超时、输出长度限制、工作目录固定。
- **为什么**：`subprocess(shell=True)` 加模型文本是严重错误；测试运行器必须执行已验证的参数数组，而不是模型构造的任意 shell 字符串。
- **验证**：允许的 pytest 命令成功运行；`rm`、重定向、管道、命令替换、网络命令、工作区外目录和超时命令被拒绝或停止。
- **完成标志**：Agent 能依据一次失败测试的输出，提出下一步，而不会获得通用终端控制权。

#### 2.4 首个编码闭环任务

- **目标**：在预置的小型练习项目中完成“阅读失败测试→定位→小改动→运行测试→总结”。
- **手写内容**：任务状态、工具轮次摘要和最终报告格式。
- **为什么**：这是从聊天 Agent 到编码 Agent 的分水岭；结果是否正确要由测试确认，而非模型自评。
- **验证**：准备一个故意失败的样例测试；记录 Agent 的每次工具调用；最后必须显示测试通过，或给出可复现的失败原因。
- **完成标志**：一次端到端任务可重复运行，并且不会改动练习工作区外的内容。

### 阶段 3：持久化、上下文与长期经验

#### 3.1 SQLite 会话与任务记录

- **目标**：将消息、工具调用、工具结果、任务状态和任务摘要保存到 SQLite。
- **手写内容**：表设计、会话创建/加载、事务、JSON 序列化、损坏记录跳过策略。
- **为什么**：会话恢复是 Hermes 的核心特征之一；持久化不能依赖进程内列表，也不能把所有状态塞进一个文本文件。
- **验证**：重启后能恢复一段会话并正确继续；损坏单条记录不阻止启动；不同会话互不串数据；迁移/重复导入有幂等保护。
- **完成标志**：可以用 CLI 指定会话 ID，并获得稳定的历史恢复提示。

#### 3.2 Prompt Builder 与上下文预算

- **目标**：按固定顺序构建系统提示词：安全规则、工作区规则、工具定义、技能、长期记忆、历史消息和任务摘要。
- **手写内容**：提示词片段、字符/令牌近似预算、最近消息保留与摘要替换策略。
- **为什么**：上下文不是越多越好。没有顺序和预算的拼接会导致模型忽略安全规则、成本失控或遗忘任务。
- **验证**：给定固定输入，提示词组成和顺序可测试；历史超阈值后只压缩旧消息并保留最近轮次；压缩失败时保留原始记录而不是丢失上下文。
- **完成标志**：长会话仍能回答已保存的关键任务事实，并能解释摘要来自何处。

#### 3.3 经授权的长期记忆

- **目标**：区分会话内容和跨会话长期记忆；只在明确 `/remember` 或同等授权后写入记忆。
- **手写内容**：记忆保存、去重、检索、注入提示词、授权命令和隐私过滤。
- **为什么**：记忆不是“模型说它记住了”。长期保存改变了隐私边界，必须由用户明确授权。
- **验证**：未授权信息重启后不可检索；授权后可跨会话检索；重复写入不产生重复项；检索不到时返回空结果；敏感模式被拒绝或要求确认。
- **完成标志**：能清楚区分“这次会话记得”与“长期保存了”。

### 阶段 4：技能、可靠性与可控扩展

#### 4.1 技能加载与任务模板

- **目标**：让一个技能以受限 Markdown/元数据形式提供任务步骤、检查项和允许工具，而不是任意可执行脚本。
- **手写内容**：技能名称校验、受限目录加载、元数据解析、技能内容注入、缺失技能错误。
- **为什么**：技能的学习价值是把可重复的工作流外置；如果技能可无约束运行代码，它会绕开核心安全策略。
- **验证**：能加载一个“修复失败测试”技能；名称非法、路径穿越、缺失技能、错误元数据被拒绝；普通聊天不会自动改变技能文件。
- **完成标志**：技能只影响提示词和允许的工作流，不自动授予新权限。

#### 4.2 权限分级、确认与审计

- **目标**：将工具分为只读、受限写入、受控测试、需确认操作；记录谁在何时以何参数请求了什么。
- **手写内容**：策略表、确认令牌/命令、审计日志、日志脱敏和拒绝原因。
- **为什么**：危险操作的正确处理不是依赖模型“自觉”，而是在执行点再次检查策略。
- **验证**：只读工具无需确认；普通写入按规则执行；需要确认的操作未确认时停止；伪造确认、过期确认、未知工具均失败；日志不出现 API Key 与文件完整敏感内容。
- **完成标志**：可从审计记录还原一次任务做了什么，同时不泄露密钥。

#### 4.3 模型错误恢复与可观测性

- **目标**：对超时、限流、无效工具 JSON、空回答和暂时性服务错误进行分类、重试或安全终止。
- **手写内容**：错误分类、指数退避、最大重试数、请求 ID/任务 ID、结构化日志。
- **为什么**：工具型 Agent 的失败并不总是代码错误；必须让使用者知道失败在模型、网络、权限还是工具本身。
- **验证**：使用假客户端稳定复现每个错误类别；可重试错误最多重试指定次数；不可重试错误立即返回；日志可按任务 ID 查询。
- **完成标志**：错误信息对用户可理解，对开发者可定位，同时没有敏感数据。

### 阶段 5：Hermes 风格的产品接口（核心完成后再做）

#### 5.1 CLI 命令与交互会话

- **目标**：提供 `hermeslite chat`、`sessions`、`memory`、`skills`、`config`、`doctor` 等清晰命令；先保证可脚本化，再优化交互体验。
- **手写内容**：`argparse` 子命令、会话选择、斜杠命令、错误退出码、`doctor` 检查。
- **为什么**：官方 Hermes 的 CLI 是能力入口，不只是一个聊天窗口；命令应调用核心服务，不重复实现业务逻辑。
- **验证**：帮助文本、未知命令、缺失参数、无效会话 ID 都有可预测结果；`doctor` 能报告配置、数据库、工作区和模型连通性。
- **完成标志**：非交互命令可被自动化测试，交互 CLI 可恢复会话并完成小型任务。

#### 5.2 可选：单进程 Gateway 与一个消息渠道

- **目标**：仅在 CLI 核心稳定后，用 Gateway 统一接入 Telegram；渠道只负责收发，Agent 核心仍只存在一份。
- **手写内容**：消息事件模型、白名单、单用户/单会话映射、Gateway 身份令牌、本地回环监听和启动/状态命令。
- **为什么**：这部分与前一项目的 Gateway 有共通性，但这里的重点是“编程任务如何被渠道转发并保持会话”，不是重复堆功能。
- **验证**：白名单外消息与群聊拒绝；Telegram 发来的任务由同一个 Agent 核心处理；渠道重启后会话仍可恢复；Gateway 不暴露到局域网。
- **完成标志**：CLI 和 Telegram 对同一会话有一致的记忆和工具安全策略。

#### 5.3 可选：计划任务与后台复盘

- **目标**：实现最小提醒/定时任务；后台复盘只生成“候选记忆或候选技能”，不能静默写入长期状态。
- **手写内容**：持久化任务、到期执行、投递接口、候选项列表和人工确认。
- **为什么**：官方 Hermes 的自我改进值得学习，但完全自动改写记忆或技能会放大模型错误。先实现可审查版本。
- **验证**：任务重启后仍存在；取消任务后不再触发；复盘候选不确认则不写入；确认后才可检索。
- **完成标志**：自动化只负责提出建议，持久知识改变仍保留控制点。

### 阶段 6：最终验收与复盘

#### 6.1 测试分层与回归基线

- **目标**：形成单元测试、工具边界测试、SQLite 集成测试、模拟模型测试和端到端练习项目测试。
- **手写内容**：测试夹具、假模型、临时工作区、覆盖关键拒绝路径的用例。
- **为什么**：真实模型输出具有不确定性；核心安全和状态逻辑必须主要由确定性测试保护。
- **验证**：全量 pytest 通过；新建虚拟环境可安装并通过测试；`pip check` 与 `git diff --check` 无错误。
- **完成标志**：至少包含一次“模型未按预期调用工具”时系统仍安全的测试。

#### 6.2 端到端毕业任务

- **目标**：在一份全新、故意带有 1–2 个简单缺陷的 Python 小项目中，让 Agent 完成受控修复。
- **任务要求**：读取任务说明；检查文件；运行失败测试；提出并执行最小补丁；重新运行测试；输出修改、验证结果、未解决风险和审计摘要。
- **验证**：全部测试通过；修改只发生在工作区；会话重启后能说明这次任务的摘要；未授权内容未进入长期记忆。
- **完成标志**：完成一份可分享的脱敏演示记录，并把核心经验写入学习日志。

## 5. 建议学习顺序与节奏

必做主线为：`0.1 → 0.2 → 1.1 → 1.2 → 1.3 → 2.1 → 2.2 → 2.3 → 2.4 → 3.1 → 3.2 → 3.3 → 4.1 → 4.2 → 4.3 → 5.1 → 6.1 → 6.2`。

`5.2` 和 `5.3` 是可选扩展，不能挤占核心编码 Agent、受限工具、测试闭环和持久化的学习时间。遇到卡点时，优先回到数据流、状态归属和工具边界，而不是继续堆叠新功能。

建议每次只完成一个二级步骤。每完成一个步骤后，先更新本计划的完成状态和 `learning-log.md`，再开始下一步；实际完成顺序若改变，应记录原因。

## 6. 与参考仓库的对应关系

| 本计划 | learn-hermes-agent 对应主题 | 官方 Hermes 对应能力 | 学习版取舍 |
| --- | --- | --- | --- |
| 阶段 1 | s01–s02 | Agent、工具与模型提供商 | 保留核心循环与结构化工具调用 |
| 阶段 2 | s02、s09、s14 | tools、terminal backends | 只保留本地受限工作区与测试命令 |
| 阶段 3 | s03–s07 | SQLite/FTS、状态、记忆 | 保留会话、摘要与授权记忆，不急于做复杂检索 |
| 阶段 4 | s08–s11、s20 | skills、permissions、logging | 保留受限技能和审计，不让技能越权 |
| 阶段 5 | s12–s15、s19 | CLI、gateway、cron | CLI 必做；消息渠道和后台复盘后置 |
| 阶段 6 | s23–s27 的评估思想 | tests、trajectory、evaluation | 先做确定性测试与端到端验收，不做 RL 管线 |

## 7. 首次执行时的起点

下一次学习从 **0.1 创建独立项目与基础环境** 开始。届时会先检查目标目录与 Git 状态，创建不含密钥的最小项目骨架，再由学习者手写环境检查与首个测试。完成验证后才更新本计划的对应状态。

